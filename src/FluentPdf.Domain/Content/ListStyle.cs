namespace FluentPdf.Domain.Content;

/// <summary>How list items are marked.</summary>
public enum ListStyle
{
    /// <summary>Unordered list with bullet markers.</summary>
    Unordered = 0,

    /// <summary>Ordered list with sequential numeric markers.</summary>
    Ordered = 1,
}
