namespace FluentPdf.Domain.Styling;

/// <summary>Horizontal alignment of a block or its content within the available width.</summary>
public enum HorizontalAlignment
{
    Left = 0,
    Center = 1,
    Right = 2,
    Justify = 3,
}
