namespace FluentPdf.Kernel;

/// <summary>
/// Marker for immutable domain events. Implementations should be <c>record</c> types so
/// they are compared by value.
/// </summary>
public interface IDomainEvent
{
}
